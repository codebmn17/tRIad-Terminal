fastapi>=0.111.0
uvicorn[standard]>=0.30.0
scikit-learn>=1.3.0
numpy>=1.24.0

# Voice (offline-first)
pyttsx3>=2.90

# Fallback TTS and sounds
playsound==1.2.2
gTTS>=2.5.1

# Optional speech input
SpeechRecognition>=3.10.0
# PyAudio often needs system libs; install_deps.py will try it:
# pyaudio
